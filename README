Adds support for HTML-like inputs in pygame. Input types are:

    - TextInput         Single line text input box

    - RadioButton       Single radio button

    - RadioGroup        Group of radio buttons

    - CheckBox          Single checkbox

    - CheckBoxGroup     Group of checkboxes

    - Form              Container for multiple form elements

Examples can be found in example.py.